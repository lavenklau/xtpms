name: xtpms
channels:
  - conda-forge
dependencies:
  - cmake>=3.16
  - ninja
  - eigen=3.4.*
  - cgal-cpp=5.6.*
  - boost-cpp>=1.74
  - gtest=1.14.*
  # OpenMesh and libigl are fetched automatically by CMake (FetchContent)
